<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="ui.controller.MainMenuController"
            style="-fx-background-color: linear-gradient(to bottom right, #667eea 0%, #764ba2 100%);">

    <!-- Top Section -->
    <top>
        <VBox alignment="CENTER" spacing="10">
            <padding>
                <Insets top="40" right="20" bottom="30" left="20"/>
            </padding>

            <Label text="📚" style="-fx-font-size: 64px;"/>
            <Label text="Library Management System"
                   style="-fx-font-size: 36px; -fx-font-weight: bold; -fx-text-fill: white; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 10, 0, 0, 2);"/>
            <Label text="Manage your library with ease and efficiency"
                   style="-fx-font-size: 14px; -fx-text-fill: rgba(255,255,255,0.9);"/>
        </VBox>
    </top>

    <!-- Center Section with 3 cards horizontally -->
    <center>
        <HBox alignment="CENTER" spacing="30">
            <padding>
                <Insets top="40" right="50" bottom="50" left="50"/>
            </padding>

            <!-- Members Card -->
            <VBox alignment="CENTER" spacing="15"
                  style="-fx-background-color: white; -fx-background-radius: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 20, 0, 0, 5);"
                  prefWidth="280" prefHeight="320">
                <padding>
                    <Insets top="30" right="30" bottom="30" left="30"/>
                </padding>

                <Label text="👥" style="-fx-font-size: 72px;"/>
                <Label text="Members" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #667eea;"/>
                <Label text="Add, view, and manage library members"
                       style="-fx-font-size: 12px; -fx-text-fill: #666; -fx-text-alignment: center;"
                       wrapText="true" maxWidth="220"/>

                <Region VBox.vgrow="ALWAYS"/>
                <Button text="Manage Members"
                        onAction="#goToMembers"
                        prefWidth="220"
                        prefHeight="45"
                        style="-fx-font-size: 14px; -fx-background-color: #667eea; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"/>
            </VBox>

            <!-- Documents Card -->
            <VBox alignment="CENTER" spacing="15"
                  style="-fx-background-color: white; -fx-background-radius: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 20, 0, 0, 5);"
                  prefWidth="280" prefHeight="320">
                <padding>
                    <Insets top="30" right="30" bottom="30" left="30"/>
                </padding>

                <Label text="📚" style="-fx-font-size: 72px;"/>
                <Label text="Documents" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #667eea;"/>
                <Label text="Browse and manage all documents (books and magazines)"
                       style="-fx-font-size: 12px; -fx-text-fill: #666; -fx-text-alignment: center;"
                       wrapText="true" maxWidth="220"/>

                <Region VBox.vgrow="ALWAYS"/>
                <Button text="Manage Documents"
                        onAction="#goToDocuments"
                        prefWidth="220"
                        prefHeight="45"
                        style="-fx-font-size: 14px; -fx-background-color: #667eea; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"/>
            </VBox>

            <!-- Borrows Card -->
            <VBox alignment="CENTER" spacing="15"
                  style="-fx-background-color: white; -fx-background-radius: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 20, 0, 0, 5);"
                  prefWidth="280" prefHeight="320">
                <padding>
                    <Insets top="30" right="30" bottom="30" left="30"/>
                </padding>

                <Label text="🔄" style="-fx-font-size: 72px;"/>
                <Label text="Borrows" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #667eea;"/>
                <Label text="Track borrowing and returns"
                       style="-fx-font-size: 12px; -fx-text-fill: #666; -fx-text-alignment: center;"
                       wrapText="true" maxWidth="220"/>

                <Region VBox.vgrow="ALWAYS"/>
                <Button text="Manage Borrows"
                        onAction="#goToBorrows"
                        prefWidth="220"
                        prefHeight="45"
                        style="-fx-font-size: 14px; -fx-background-color: #667eea; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"/>
            </VBox>

        </HBox>
    </center>

    <!-- Bottom Section -->
    <bottom>
        <HBox alignment="CENTER">
            <padding>
                <Insets bottom="20"/>
            </padding>
            <Label text="© 2025 Library Management System"
                   style="-fx-font-size: 11px; -fx-text-fill: rgba(255,255,255,0.7);"/>
        </HBox>
    </bottom>

</BorderPane>